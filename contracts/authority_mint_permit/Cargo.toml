[package]
name = "authority-mint-permit"
version = "1.0.0"
edition = "2021"

[dependencies]
casper-contract = "5.1.1"
casper-types = "6.1.0"
blake2 = { version = "0.10", default-features = false }

[lib]
crate-type = ["cdylib"]

[profile.release]
codegen-units = 1
opt-level = "z"
lto = true
