# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_NETWORK=devnet

# Backend Ed25519 Keypair (base58 encoded secret key)
# Generate with: solana-keygen new --no-bip39-passphrase --outfile backend-keypair.json
# Then: cat backend-keypair.json | jq -r '.[0:32] | @base58'
BACKEND_SECRET_KEY=your_base58_secret_key_here

# Program IDs
RWA_RAFFLE_PROGRAM_ID=5xAQW7YPsYjHkeWfuqa55ZbeUDcLJtsRUiU4HcCLm12M
DIRECT_SELL_PROGRAM_ID=Di1ectSe11111111111111111111111111111111111
RWA_REDEEM_PROGRAM_ID=RwaRede3m1111111111111111111111111111111111

# Server Configuration
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/mogate_rwa

# Redis (for rate limiting and caching)
REDIS_URL=redis://localhost:6379

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# KYC/Whitelist
ENABLE_KYC_CHECK=false
ENABLE_WHITELIST_CHECK=false
