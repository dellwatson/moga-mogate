[package]
name = "multi_raffle"
version = "0.1.0"
edition = "2021"
description = "Multi-raffle Anchor program (SOL-based, unsafe routes only)"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib", "lib"]
name = "multi_raffle"

[features]
no-entrypoint = []
no-idl = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = { version = "0.29.0", features = ["metadata"] }
mpl-token-metadata = "4.1.2"
# Light Protocol dependencies - will add when available
# light-sdk = "0.12.0"
# light-compressed-pda = "0.7.0"
# light-hasher = "0.7.0"
# light-merkle-tree-reference = "0.7.0"
